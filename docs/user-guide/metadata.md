# Metadata Files

Current provenance records are written as versioned JSON files with the
`.metadata.json` suffix.

See [Reproducibility](reproducibility.md) for the schema, reproduction recipe,
and versioning rules.
